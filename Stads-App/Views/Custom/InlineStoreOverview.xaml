<UserControl
    x:Class="Stads_App.Views.Custom.InlineStoreOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Stads_App.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    d:DataContext="{d:DesignInstance models:Store}">

    <StackPanel Tapped="GoToStore">
        <TextBlock TextAlignment="Center" Text="{Binding Name}" Style="{StaticResource TitleTextBlockStyle}"
                   Padding="0, 0, 0, 10" />

        <Image HorizontalAlignment="Center" Source="{Binding ImgPath}" Width="200" />
    </StackPanel>
</UserControl>